---
name: image-to-pcb-art
description: 将插画或线稿制作成嘉立创EDA彩色丝印与露铜金属效果的PCB艺术板；用于图片分层、导入、轮廓平滑、图层对齐、制造文件检查，以及排查丝印或阻焊边缘毛刺。普通电子电路设计与审查不适用。
---

# 图片转 PCB 艺术板

将画面拆成彩色印刷和金属图形，再按实际板面叠加。以用户指定的尺寸、正反面内容和板厂工艺为准；示例工程的尺寸或图形数量不能当成默认值。

## 开始前

确认目标是新建艺术板、改造现有 PCB，还是只需分析流程。取得源图、成品尺寸、正反面方向、需要呈现金属的区域和已有工程。修改前检查当前板框与图层：若目标图形已存在，先比对，再决定替换或补充，避免重复叠加。保留可回退的工程版本。

在嘉立创EDA前台操作时，使用已安装的 `easyeda-api` Skill 查询窗口、文档和 API，再按其文档执行；不要臆造接口或写死窗口 ID。没有可用连接时，可用嘉立创EDA界面完成同一流程。

## 制作流程

1. **规划画面和板框。** 按用户成品尺寸制作正面、背面彩色图，将露铜线稿或装饰单独提取为高对比度蒙版。考虑板边、圆角、孔位、正反面翻转和文字阅读方向。金属区域应从同一原始坐标系生成，避免各层各自缩放。
2. **设置彩色丝印。** 在「设置 → PCB → 通用」开启「使用嘉立创彩色丝印工艺」。用「放置 → 图片」导入彩色 PNG/JPG，预览中选「放置原图」；默认放在顶层丝印，需要背面时改为底层丝印。设定毫米尺寸和位置，使画面与板框相符。原图导入与转矢量是两种用途，不要把整张彩色图按黑白轮廓转换。顶层彩色丝印默认使用完整原图；即使同一区域另有顶层铜和阻焊开窗，也不要自动从丝印图片中扣除这些区域。
3. **制作金属图形。** 将需露出的装饰转为闭合矢量区域，放到相应铜层，并在同面阻焊层做对应开窗。铜与开窗应从同一份已清理的轮廓派生，按实际叠层确认露出范围；底面另核对镜像。不要只放阻焊开窗而缺少下方铜，也不要以编辑器显示的铜层颜色推断成品金属颜色。
4. **处理边缘。** 导入黑白蒙版时，结合预览调节容差、简化和平滑；过度简化会丢失细节。优先在矢量化前清除孤立像素、锯齿和不必要的小孔。遇到直线段拼出的弧线折角，可重建平滑曲线。只有用户明确要求丝印让出金属区域时，才制作透明区；透明区应按最终金属轮廓制作，并检查没有多余空洞或残留边缘像素。
5. **核对叠层。** 分层检查板框、彩色丝印、铜与阻焊；确认位置、尺寸、镜像和金属轮廓一致；仅在要求丝印透明时核对透明区。放大检查曲线、细线、尖角与小岛是否可制造，并按当前板厂规则确认最小线宽和间距。用 2D/3D 预览选择彩色丝印工艺，逐面检查整体效果。
6. **保存与导出。** 保存可编辑工程，导出「PCB制板文件(Gerber)」时勾选彩色丝印。核对压缩包同时含所需常规铜层、阻焊文件和彩色丝印专用文件；嘉立创EDA示例中正反面文件扩展名为 `.FCTS`、`.FCBS`。需要交付制造时，再核对板厂当前的彩色丝印、板材、阻焊颜色和表面处理要求，以及提交后的生产预览。

## 修改现有艺术板

只改用户指定的图层与区域。若重做金属边缘，先保存旧工程，用同一最终路径更新铜层与对应阻焊开窗；若用户明确要求重新制作丝印透明区，须保留原彩图中未指定部分；否则保持完整彩图。写回后核对新旧对象没有重叠、铜与阻焊轮廓仍对齐、非目标图层未变化，并重新保存工程。若出现画质退化或额外空洞，从合格版本重做，不沿用有缺陷的中间结果。

## 资料与适用边界

- [嘉立创EDA：绘制彩色丝印](https://prodocs.lceda.cn/cn/pcb/other-draw-colorful-silkscreen/)：设置、原图导入、预览、导出与下单条件。下单条件可能变化，以操作时的官方说明为准。
- [嘉立创EDA：放置图片](https://prodocs.lceda.cn/cn/pcb/place-image/index.html)：格式、容差、简化、平滑、尺寸、图层和镜像。
- [DeepSeek-PCB-Art 示例工程](https://github.com/alsunmengy/DeepSeek-PCB-Art/tree/main)：分层叠加的实例；它的板尺寸、图形数量和文件布局仅是个案，不作为通用规格。
